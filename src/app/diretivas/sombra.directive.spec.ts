import { SombraDirective } from './sombra.directive';

describe('SombraDirective', () => {
  it('should create an instance', () => {
    const directive = new SombraDirective();
    expect(directive).toBeTruthy();
  });
});
